# pr-screenshots

PR の説明に貼るスクリーンショット置き場。**main にはマージしない** orphan ブランチ。
コードとは無関係なので、不要になったら消してよい。

- `2026-09-08/` — #491 / #492 / #494 の before（main）/ after（各ブランチ統合）。
  375×812 @2x（スマホ）と 1280×900（デスクトップ）。ローカルのシードデータ。
- `2026-09-09-492-modes/` — #492 の表示モード比較。`<幅>-<言語>-<モード>[-image].png`（トップ。`-image` は文字入り画像のカードを先頭に）、
  `profile-w320-<言語>-<モード>.png`（プロフィールの下書きタブ。役割・下書きチップあり）。
